# History index

这里保存已完成使命的控制研究：grouped PD、Acceleration/Force NF scan、explicit PD、J6 重力裕量、旧 short-motion 和 17.78 s LIFT-only 阶段验证。

这些入口只用于复盘，不是当前推荐配置。正式控制合同是 `runtime/config/full_pick_place_25s_dog_candidate3800.json`；正式结果是 `../outputs/full_pick_place_25s_dog_candidate3800/`。

`legacy_route_c_cpu_mesh/` 保存已退出 production path 的 SciPy/Pinocchio IK、coarse reachability 和完整 mesh/table collision 工具；不得把其中 coarse FAIL 当作 exact physical unreachable。

`evidence/verified_slow_baseline_dog3800/` 中的旧路径记录属于历史原始事实；目录移动后的正式入口请以根 README 为准。
