# Route-A cases

The five `scene_*` directories contain retained prediction/filter products.
`selected_native_case/` is the single overwriteable generation workspace and
has status `TEMPORARY_REGENERABLE`; it is not the formal success record.

The only authoritative Route-A physical baseline is:

```text
../04_verified_baseline/scene0000_view0001_ashtray_source0462/
```

Its `05_isaacsim/final_result.json` and `SHA256SUMS` define the frozen result.
